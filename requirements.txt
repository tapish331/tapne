# Core Django runtime
Django>=5.1,<6.0
gunicorn>=22.0,<23.0
python-dotenv>=1.0,<2.0

# Database
psycopg[binary]>=3.1,<4.0
dj-database-url>=2.2,<3.0

# Static and storage
whitenoise[brotli]>=6.7,<7.0
django-storages[s3]>=1.14,<2.0
boto3>=1.35,<2.0

# Cache and queue
redis>=5.0,<6.0
django-redis>=5.4,<6.0
celery>=5.4,<6.0

# Content and media safety (blogs + uploads)
Pillow>=10.4,<12.0
Markdown>=3.6,<4.0
bleach>=6.1,<7.0

# Accounts and identity
email-validator>=2.2,<3.0

# Feed app note:
# home feed ranking, personalization, and bootstrap_feed command use Django core
# and do not require additional third-party packages.

# Search app note:
# search defaults, ranking logic, and bootstrap_search command rely on Django core
# plus existing feed/account models. No extra third-party package is required.

# Trips app note:
# trips list/detail/CRUD, mine tabs, and bootstrap_trips command rely on Django core
# plus existing account/feed models. No extra third-party package is required.

# Blogs app note:
# blogs list/detail/CRUD and bootstrap_blogs command rely on Django core
# plus existing account/feed models. No extra third-party package is required.

# Social app note:
# follow/bookmark routes, bookmarks page payloads, and bootstrap_social command
# rely on Django core plus existing account/feed/trip/blog models.
# No additional third-party package is required.

# Enrollment app note:
# join-request routes, host inbox review flows, and bootstrap_enrollment command
# rely on Django core plus existing trip/account models.
# No additional third-party package is required.

# Interactions app note:
# comment/reply routes, DM inbox/thread/send flows, and bootstrap_interactions command
# rely on Django core plus existing trip/blog/account/feed models.
# No additional third-party package is required.

# Reviews app note:
# review create/list routes, target payload aggregation, and bootstrap_reviews command
# rely on Django core plus existing trip/blog/account/feed models.
# No additional third-party package is required.

# Activity app note:
# activity timeline aggregation and bootstrap_activity command rely on Django core
# plus existing social/enrollment/interactions/reviews/trip/blog/account models.
# No additional third-party package is required.

# Settings app note:
# member settings model/form/views and bootstrap_settings command rely on Django core
# plus existing auth/account models. No additional third-party package is required.

# Media app note:
# media upload/attachment flows, validation, and bootstrap_media command rely on
# Django core plus existing Pillow + storage dependencies above.
# No additional third-party package is required.

# Runtime app note:
# runtime cache shelves (feed/search), rate limiting, idempotency guards,
# runtime counters, and background task broker shelf utilities rely on
# Django core plus existing redis/django-redis/celery dependencies above.
# No additional third-party package is required.
google-cloud-storage>=2.18,<3.0
