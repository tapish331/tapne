# Core Django runtime
Django>=5.1,<6.0
gunicorn>=22.0,<23.0
python-dotenv>=1.0,<2.0

# Database
psycopg[binary]>=3.1,<4.0
dj-database-url>=2.2,<3.0

# Static and storage
whitenoise[brotli]>=6.7,<7.0
django-storages[s3]>=1.14,<2.0
boto3>=1.35,<2.0

# Cache and queue
redis>=5.0,<6.0
django-redis>=5.4,<6.0
celery>=5.4,<6.0

# Content and media safety (blogs + image uploads)
Pillow>=10.4,<12.0
Markdown>=3.6,<4.0
bleach>=6.1,<7.0

# Accounts and identity
email-validator>=2.2,<3.0

# Feed app note:
# home feed ranking, personalization, and bootstrap_feed command use Django core
# and do not require additional third-party packages.

# Search app note:
# search defaults, ranking logic, and bootstrap_search command rely on Django core
# plus existing feed/account models. No extra third-party package is required.

# Trips app note:
# trips list/detail/CRUD, mine tabs, and bootstrap_trips command rely on Django core
# plus existing account/feed models. No extra third-party package is required.

# Blogs app note:
# blogs list/detail/CRUD and bootstrap_blogs command rely on Django core
# plus existing account/feed models. No extra third-party package is required.

# Social app note:
# follow/bookmark routes, bookmarks page payloads, and bootstrap_social command
# rely on Django core plus existing account/feed/trip/blog models.
# No additional third-party package is required.
